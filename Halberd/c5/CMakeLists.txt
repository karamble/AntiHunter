# Halberd C5 firmware — ESP-IDF v6.0.1 project.
# Built via `make c5-build` from the repo root, which sources IDF's export.sh
# before invoking idf.py.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(halberd_c5)
